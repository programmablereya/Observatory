<!DOCTYPE html>
<html>
<head>
  <title><%= (content_for?(:title) ? (content_for(:title) + " : ") : "") + "The Observatory" %></title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<%= yield %>

</body>
</html>
