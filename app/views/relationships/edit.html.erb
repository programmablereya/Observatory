<% content_for :title, "Editing relationship between #{@character.name} and #{@relationship.target.name}" %>
<h1>Editing relationship</h1>

<%= render 'form' %>

<%= link_to 'Show', [@character,@relationship] %> |
<%= link_to 'Back', character_relationships_path(@character) %>
